Read the plan at `docs/plans/$ARGUMENTS.md`.

Follow it exactly. Do not deviate from the approved plan.

For each step in the plan:
1. Write the failing test(s) first
2. Run the tests — confirm they fail
3. Write the minimum code to make them pass
4. Run the tests — confirm they pass
5. Run the linter and fix any issues

After completing all steps, run the full test suite and report results.

If you encounter something the plan didn't account for, STOP and ask me before proceeding. Do not improvise outside the plan.
